{
  "name": "roweb",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "description": "MMORPG web prototype with original cleric sprites, Sanctuary, procedural audio, attributes, collisions and a pixel fantasy map.",
  "scripts": {
    "dev": "node tools/dev-server.mjs",
    "build": "node scripts/build.mjs",
    "check": "node --check tools/dev-server.mjs && node --check src/game.js && node --check src/roweb2.js && node --check src/roweb3.js && node --check src/roweb4-addon.js && node --check scripts/build.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
