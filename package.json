{
  "name": "roweb",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MMORPG web prototype with an original procedural presentation.",
  "scripts": {
    "dev": "node tools/dev-server.mjs",
    "build": "node scripts/build.mjs",
    "check": "node --check tools/dev-server.mjs && node --check src/game.js && node --check scripts/build.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
