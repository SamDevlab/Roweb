{
  "name": "roweb",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MMORPG web prototype with an original procedural presentation.",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "build": "node scripts/build.mjs",
    "check": "node --check server.mjs && node --check src/game.js && node --check scripts/build.mjs"
  },
  "dependencies": {
    "express": "^5.1.0",
    "ws": "^8.18.3"
  },
  "engines": {
    "node": ">=18"
  }
}
